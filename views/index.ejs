<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Document</title>
        <link
            href="
        https://cdn.jsdelivr.net/npm/reset-css@5.0.2/reset.min.css
        "
            rel="stylesheet"
        />

        <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css"
            integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg=="
            crossorigin="anonymous"
            referrerpolicy="no-referrer"
        />
        <script src="https://cdn.tailwindcss.com"></script>
    </head>
    <body class="w-full h-screen relative">
        <!-- error -->
        <%- include('./partials/flash-error') %>

        <div class="size-full flex justify-center items-center gap-28">
            <div class="w-1/3">
                <h3 class="text-3xl font-medium">
                    welcome to
                    <span class="font-bold text-blue-400">Scatch</span>
                </h3>

                <!-- form register -->
                <p class="text-xl">Create Your Account</p>
                <form
                    action="/users/register"
                    method="post"
                    class="mt-4 flex flex-col gap-3"
                >
                    <input
                        type="text"
                        class="outline-none bg-slate-100 p-3 rounded-md border-slate-200 border-2 w-full"
                        placeholder="Full Name"
                        name="fullname"
                    />
                    <input
                        type="email"
                        class="outline-none bg-slate-100 p-3 rounded-md border-slate-200 border-2 w-full"
                        placeholder="Email"
                        name="email"
                    />
                    <input
                        type="password"
                        class="outline-none bg-slate-100 p-3 rounded-md border-slate-200 border-2 w-full"
                        placeholder="Password"
                        name="password"
                    />
                    <input
                        type="submit"
                        value="Create My Account"
                        class="bg-blue-500 rounded-full px-4 py-3 w-fit text-white mt-1"
                    />
                </form>
            </div>
            <div class="w-1/3">
                <!-- form login -->
                <p class="text-xl">Login Your Account</p>
                <form
                    action="/users/login"
                    method="post"
                    class="mt-4 flex flex-col gap-3"
                >
                    <input
                        type="email"
                        class="outline-none bg-slate-100 p-3 rounded-md border-slate-200 border-2 w-full"
                        placeholder="Email"
                        name="email"
                    />
                    <input
                        type="password"
                        class="outline-none bg-slate-100 p-3 rounded-md border-slate-200 border-2 w-full"
                        placeholder="Password"
                        name="password"
                    />
                    <input
                        type="submit"
                        value="Login"
                        class="bg-blue-500 rounded-full px-4 py-3 w-fit text-white mt-1"
                    />
                </form>
            </div>
        </div>
    </body>
</html>
